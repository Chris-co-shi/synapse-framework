# 06-待补充问题

## 1. 技术问题

- 是否需要单独拆分 `synapse-cloud-*` adapter 子模块？
- Gateway adapter 是否进入 v0.1，还是只做接口预留？
- Seata 是否作为独立 adapter，还是由消费方自行接入？
- 文件存储是否第一阶段只做 Port，不做具体实现？
- 任务调度是否优先适配 Spring Task，还是预留 XXL-Job / Quartz？

## 2. 版本问题

- Spring Boot 3.5.x 升级补丁版本策略。
- Spring Cloud 2025.0.x service release 跟进策略。
- Spring Cloud Alibaba 与 Seata/RocketMQ 版本冲突处理。

## 3. 工程问题

- 是否需要单独 `synapse-test` 测试支撑模块？
- 是否需要发布 internal BOM 和 public BOM？
- 是否需要生成模块 API 文档站点？

## 4. 规则问题

- 哪些公共 API 标记为稳定契约？
- 哪些 SPI 允许 breaking change？
- Skill 更新是否纳入每个模块验收。
